services:
  postgres:
    # Using pgvector image for vector similarity search support
    image: pgvector/pgvector:pg16
    container_name: agent-framework-db
    environment:
      POSTGRES_DB: agent_framework
      POSTGRES_USER: agent_user
      POSTGRES_PASSWORD: agent_pass
    ports:
      - "${DB_PORT:-5433}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U agent_user -d agent_framework"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ML Prediction Service (Python) - will be implemented later
  # ml-service:
  #   build: ./ml-service
  #   container_name: agent-framework-ml
  #   environment:
  #     DATABASE_URL: postgresql://agent_user:agent_pass@postgres:5432/agent_framework
  #     MODEL_PATH: /app/models
  #   ports:
  #     - "8001:8001"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   volumes:
  #     - ml_models:/app/models

volumes:
  postgres_data:
  # ml_models: